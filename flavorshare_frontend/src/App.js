import React from 'react';

/**
 * PUBLIC_INTERFACE
 * App
 * This component is retained for tests but the app now mounts RoutesApp at index.js.
 */
function App() {
  /** Minimal component kept for CRA test compatibility. */
  return (
    <div className="App">
      <p>FlavorShare app is running.</p>
    </div>
  );
}

export default App;
