import Header from './ui/Header.jsx';
/**
 * PUBLIC_INTERFACE
 * Header
 * Compatibility re-export to the redesigned Header component.
 */
export function Header() {
  /** Delegates to redesigned Header implementation. */
  return <Header />;
}
