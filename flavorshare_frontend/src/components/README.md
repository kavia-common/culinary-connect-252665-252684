# Components Notes

- SidebarFilters: retained for future use but currently unused. The UI does not render filters and no tagId is passed to recipesApi.list from pages. To re-enable, import SidebarFilters in pages and plumb selectedTag to recipesApi.list.
